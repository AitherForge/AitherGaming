# Local Games

This directory contains the complete game files bundled with Aither Gaming.

Each game is stored in its own directory and has its own `index.html` entry point. The importer populates this directory from the supplied game collections.
