let games=[];
const grid=document.getElementById('gameGrid'),search=document.getElementById('search'),count=document.getElementById('count'),filters=document.getElementById('filters');
let active='All';
async function init(){games=await fetch('games.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw Error('games.json failed');return r.json()});const categories=['All',...new Set(games.map(g=>g.category))];filters.innerHTML=categories.map(c=>`<button class="chip ${c==='All'?'active':''}" data-category="${c}">${c}</button>`).join('');filters.addEventListener('click',e=>{const b=e.target.closest('.chip');if(!b)return;active=b.dataset.category;document.querySelectorAll('.chip').forEach(x=>x.classList.toggle('active',x===b));render()});search.addEventListener('input',render);render()}
function render(){const q=search.value.trim().toLowerCase(),visible=games.filter(g=>(active==='All'||g.category===active)&&(!q||`${g.name} ${g.category}`.toLowerCase().includes(q)));count.textContent=`${visible.length} games ready to play`;grid.innerHTML=visible.length?visible.map(g=>`<a class="game-card" href="${g.path}/"><div class="game-icon">${g.icon}</div><h3>${g.name}</h3><p>${g.category}</p><span class="play">Play game →</span></a>`).join(''):`<div class="game-card" style="grid-column:1/-1;text-align:center"><h3>No games found</h3><p>Try another search.</p></div>`}
const themeButton=document.getElementById('themeButton'),saved=localStorage.getItem('aither-theme');if(saved==='light')document.body.classList.add('light');themeButton.onclick=()=>{document.body.classList.toggle('light');localStorage.setItem('aither-theme',document.body.classList.contains('light')?'light':'dark')};init().catch(()=>count.textContent='Game library failed to load');